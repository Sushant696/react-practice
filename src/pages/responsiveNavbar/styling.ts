import styled from "styled-components";
import { Menu } from 'antd';

export const StyledMenu = styled(Menu)`
//   background-color: #f0f2f5; /* Customize the background color */
  border: none;
  display: flex;
  justify-content: flex-end;
`;